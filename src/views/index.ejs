<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Formulario</title>
  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
  <link rel="style" href="stylesheets/style.css">
</head>
<body>
  <div class="container">
    <% if (session.user) { %>
      <style>
        body{
          background-color: <%= session.user.color %>;
        }
      </style>
      <div class="text-center mt-3 text-uppercase">
        <h1 class="mb-5 bd-highlight border border-3 border-danger p-2">Hola <%= session.user.nombre %> </h1>
        <h2 class="border-bottom border-top border-danger border-3 mb-2 p-3">Elegiste el color: <%= session.user.color %> </h2>
        <h2 class="border-bottom border-top border-danger border-3 mb-2 p-3">Tu email es: <%= session.user.email %> </h2>
        <h2 class="border-bottom border-top border-danger border-3 mb-2 p-3">Y tu edad es: <%= session.user.edad = !undefined ? session.user.edad + " Años" : "No lo indicaste" %> </h2>
        <a class="btn btn-primary mt-3" href="/user">Siguiente</a>
      </div>
      
   <% } else { %> 
      <form class="row g-3 mt-3" action="" method="post">
        
        <div class="col-6">
          <label for="nombre" class="form-label">Ingrese su nombre</label>
          <input type="text" name="nombre" id="nombre" class="form-control">
          <small class="text-danger ms-2"><%= locals.errors && errors.nombre ? errors.nombre.msg : null%></small>
        </div>

        <div class="col-6">
          <label for="email" class="form-label">Ingrese su email</label>
          <input type="email" name="email" id="email" class="form-control">
          <small class="text-danger ms-2"><%= locals.errors && errors.email ? errors.email.msg : null%></small>
        </div>

        <div class="col-6">
          <label for="edad" class="form-label">Igrese su edad</label>
          <input type="number" name="edad" id="edad" class="form-control">
          <small class="text-danger ms-2"><%= locals.errors && errors.edad ? errors.edad.msg : null%></small>
        </div>
  

        <div class="col-1">
          <label for="color"  class="form-label">Colores</label>
          <input type="color" class="form-control" name="color" id="color">
        </div>
 
        <div class="col-12">
          <label for="recordar">Recordar</label>
          <input type="checkbox" name="recordar" id="recordar">
        </div>
        
        <div class="col-12">
          <button type="submit" class="btn btn-primary">Enviar</button>
        </div>
      </form>
    <% } %>
  </div>
</body>
</html>
